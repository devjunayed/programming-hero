/** Practice task 2
 * Write a function called make_avg() which will take an three integers and return the 
 * average of those values * 
 */


function make_avg(first, second, third){
    return (first + second + third ) / 3;
}

var average = make_avg(20, 30, 10);
console.log(average);